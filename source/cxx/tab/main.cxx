/*
    tab (or `spacetab`): command-line tool
    \ to convert spaces into tabs but only
    \ for lines that starting with spaces.
*/
// includes
// -- std
#include <iostream>

int main( int argc, char *argv[] ) {
    std::cout << argv[argc - argc] << std::endl;
}
